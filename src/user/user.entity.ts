export type createUser = {
  name: string;
  age: number;
};

// ใช้ได้เหมือนกัน
// export class updateUser {
//   name?: string;
//   age?: number;
// }

// ใช้ได้เหมือนกัน
export type updateUser = {
  name?: string;
  age?: number;
};

// ใช้ได้เหมือนกัน
// export interface updateUser {
//   name?: string;
//   age?: number;
// }
